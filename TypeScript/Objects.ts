/*Objects: 
It is a collection of key and value pair - {key:value} available inside curly braces
It was introduced when Javscript & typescript camein

Literal: when u try to define a value directly to a variable then it is literal

1. Object Literal
     when u try to define a value directly to a variable like - {key : value}
     Syntax:
        const obj = {key : value, key1:value1}

    There are 2 ways in which we can fetch the value from object literal
    1. obj.key
            eg: const human = {
            
            name : "John",
            age : 40,
            gender : Male
            salary : 10000
            address : {
             ksr home,street
            },
            skills : ["java", "Python"]
            
            }

            //1.fetching the value from object
            console.log(human.name)  //obj.key
            console.log(human.address.city)
            //2.
            console.log(human["age"])
            console.log(human.skills[1])


    2. obj["key"]
    Note: key will be stored internally in the form of "String"

     2. define a class and create an object



*/

/*Class
    create an object for a class to access properties and methods
    Introduced in 2015 - ES6 - ECMA SCRIPT 6


*/